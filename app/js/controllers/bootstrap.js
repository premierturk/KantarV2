'use strict';

/* Controllers */
app.controller('navCtrl', function ($scope, $linq, $http, $timeout, $log, kendoExt) {
  
})

app.controller('HeaderCtrl', ['$scope', '$http', '$state', '$localStorage', '$modal', function ($scope, $http, $state, $localStorage, $modal) {
  
}])
